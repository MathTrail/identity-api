terraform {
  required_version = ">= 1.5"
}

# Identity databases (kratos, hydra, keto) are managed by Ory Helm charts.
# On-prem: databases provisioned via mathtrail-infra-local (shared PostgreSQL).
# This file exists as a scaffold for future on-prem resources.
